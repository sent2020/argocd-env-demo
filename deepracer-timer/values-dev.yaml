fullnameOverride: deepracer-timer

profile: dev

namespace: sample-dev

image:
  repository: nalbam/deepracer-timer
  tag: v0.0.9

ingress:
  enabled: true
  hosts:
    - deepracer-timer-dev.demo.mzdev.be
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    certmanager.k8s.io/cluster-issuer: "letsencrypt-staging"
  tls:
    - secretName: deepracer-timer-tls
      hosts:
        - deepracer-timer-dev.demo.mzdev.be

configmap:
  enabled: true
  data:
    MESSAGE: "hello dev world"

secret:
  enabled: true
  data:
    SECRET: "c2VjcmV0IHdvcmxkCg=="
