kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: coruscant:config-admin
rules:
- apiGroups: [""]
  resources: ["configmaps", "secrets"]
  verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
