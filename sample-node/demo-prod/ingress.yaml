apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  labels:
    app: sample-node
    app.kubernetes.io/name: sample-node
  name: sample-node
spec:
  rules:
    - host: sample-node.bruce.spic.me
  tls:
    - hosts:
        - sample-node.bruce.spic.me
      secretName: sample-node-tls
